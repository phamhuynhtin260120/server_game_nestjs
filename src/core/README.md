# Core

App-wide infrastructure lives here.

Use this folder for shared application setup that affects the whole server, such as authentication, configuration, Redis, cache, mail, database providers, guards, interceptors, and global modules.

Avoid putting domain-specific game, user, payment, or account business logic here.
