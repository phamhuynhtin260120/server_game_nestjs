# Events

Domain event publishers and listeners live here.

Use this folder for event names, event payloads, event emitters, subscribers, listeners, and cross-module reactions such as `UserCreated`, `MatchFinished`, or `PaymentCompleted`.

Keep event payloads explicit and stable because they often connect multiple modules.
