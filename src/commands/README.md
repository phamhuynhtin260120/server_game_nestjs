# Commands

CLI jobs, scheduled jobs, and CRON logic live here.

Use this folder for maintenance scripts, queue workers, recurring tasks, data backfills, cleanup jobs, and command-style application tasks.

Keep HTTP request logic out of this folder; commands should be runnable independently from controllers.
