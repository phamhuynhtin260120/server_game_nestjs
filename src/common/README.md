# Common

Generic reusable utilities live here.

Use this folder for decorators, pipes, filters, DTO helpers, shared types, constants, validators, pagination helpers, and utilities that can be reused by many modules.

Keep this folder framework-light and domain-neutral when possible.
